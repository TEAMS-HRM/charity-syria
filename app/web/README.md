# CharityApp Web (React)

Separated frontend application for CharityApp.

## Local development

```powershell
npm install
npm run dev
```

## Build

```powershell
npm run build
```

The build output is generated in `dist/` and deployed by frontend-specific
GitHub Actions workflows.
